import {
  GET_ALL_CLIENTS,
  GET_ORDERS_SINGLE_USER,
  GET_ALL_ORDERS,
  GET_USER,
  GET_ALL_PRODUCTS,
} from "./types";

export default (state,action) => {

    switch(action.type){

            

    }

}